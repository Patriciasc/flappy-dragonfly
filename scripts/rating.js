class rating extends Phaser.Scene {
    constructor() {
        super("ratingS");
    }

    create() {
        var text = this.add.text(20, 20, "Loading raiting...");
        //load next level
    }
}